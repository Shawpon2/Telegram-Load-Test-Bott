# ============================================
# BSB ULTIMATE LOAD TEST BOT - REQUIREMENTS
# Python 3.8+ required
# ============================================

# Core async & network libraries
uvloop>=0.19.0            # Fastest event loop (Linux/macOS)
aiohttp>=3.9.0            # HTTP client/server for asyncio
httpx>=0.27.0             # Async HTTP client with HTTP/2 support
requests>=2.31.0          # Sync HTTP client
grequests>=0.7.0          # Async requests using gevent
curl_cffi>=0.5.10         # Curl-impersonate – bypass TLS fingerprint
websockets>=12.0          # WebSocket client/server

# Concurrency & greenlets
gevent>=24.2.1            # Greenlet-based concurrency
eventlet>=0.36.0          # Eventlet networking

# Telegram bot libraries
python-telegram-bot>=20.7 # Telegram Bot API wrapper
httpx[http2]>=0.27.0      # For HTTP/2 support in bot requests

# Optional: browser automation (very heavy, enable only if needed)
# playwright>=1.42.0
# selenium>=4.18.0

# ========== PLATFORM-SPECIFIC NOTES ==========
# - uvloop is optional on Windows; it will be skipped automatically.
# - curl_cffi requires libcurl and a C compiler; on Termux install libcurl.
# - For gevent/eventlet, you may need to install libevent-dev on Linux.